# GCP Configuration (required for fallback mode, or if not using GitHub)
GOOGLE_APPLICATION_CREDENTIALS= #path to your credentials file only local development
BUCKET_NAME= #GCP bucket name
GCP_PROJECT= #project id
REPO_JSON_FILE=repo.json #filename for parameters repo

# BigQuery Configuration
BQ_DATASET=dd_events_validator_logs #BigQuery dataset name
BQ_TABLE=dd_events_validator_logs_table #BigQuery table name

# GitHub Schema Repository Configuration (optional - enables GitHub mode)
# When configured, schemas are read from and written to GitHub instead of GCS directly.
# GitHub Actions will sync changes to GCS automatically.
GITHUB_TOKEN= #Personal access token or GitHub App token with repo access
SCHEMA_REPO_OWNER= #GitHub organization or username (e.g., "defuseddata")
SCHEMA_REPO_NAME= #Repository name (e.g., "event-schemas")
SCHEMA_REPO_PATH=schemas #Path to schemas folder within the repo (default: schemas)
SCHEMA_REPO_DEFAULT_BRANCH=main #Default branch (default: main)
